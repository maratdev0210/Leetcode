#include <bits/stdc++.h>

using namespace std;

class Solution {
    public:
        int smallestRangeI(vector<int>& nums, int k) {
            int mi = 100000000;
            int n = nums.size();
            sort(nums.begin(), nums.end());
            
        }
    };